name: CI

on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main, develop]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install confluent-kafka jiwer
      - name: Lint placeholder
        run: |
          echo "No code yet - placeholder lint"
      - name: Run metrics scripts smoke test
        run: |
          make asr
      - name: Generate SBOM (placeholder)
        run: |
          echo "SBOM generation placeholder"
